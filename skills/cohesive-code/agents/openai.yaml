interface:
  display_name: "Cohesive Code"
  short_description: "Build and operate ccode workspaces"
  default_prompt: "Use $cohesive-code to work with this Cohesive Code workspace, from configuration and authoring through generation, accelerators, validation, and conflict resolution."

policy:
  allow_implicit_invocation: true
