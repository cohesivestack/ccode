interface:
  display_name: "Cohesive Code"
  short_description: "Build or fix cohesive-code workspaces"
  default_prompt: "Use $cohesive-code to inspect, extend, or repair this ccode workspace, validate generation flows, and handle accelerator adjustment workflows."

policy:
  allow_implicit_invocation: true
